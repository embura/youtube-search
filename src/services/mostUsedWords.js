
/**
 * 
 * @param {Array<string>} listWorld 
 * @param {number} quantity 
 * @returns {Array<string>}
 */
const findTopWords = (listWorld = [], quantity = 5) => {
    return [];
}


module.exports = {
    findTopWords
}